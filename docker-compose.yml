version: '3.8'
services:
  marrooks:
    build: .
    container_name: marrooks
    ports:
      - "8080:80"
    volumes:
      - .:/usr/share/nginx/html
    restart: unless-stopped      
    networks:
    - proxy

networks:
    proxy:
        external: true